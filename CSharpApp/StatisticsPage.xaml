<Page x:Class="ToursApp.StatisticsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Статистика обращений">

    <Grid Margin="20" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Статистика обращений граждан" FontSize="26"  Foreground="White"
               HorizontalAlignment="Left" Margin="-10,16,0,4"/>

        <Label Grid.Row="1" Content="Всего обращений:" FontSize="20"  Margin="0,10,0,0"/>
        <Label Grid.Row="2" Name="tbTotalCount" Content="0" FontSize="28" FontWeight="Bold" 
               Foreground="#008000" Margin="0,5,0,0"/>


        <Label Grid.Row="3" Content="Распределение по статусам:" FontSize="20"  Margin="0,20,0,0"/>
        <ListBox Grid.Row="4" Name="lbByStatus" Height="120" Margin="0,5,0,0" FontSize="14"/>


        <Label Grid.Row="5" Content="Распределение по сотрудникам:" FontSize="18"  Margin="0,20,0,0"/>
        <ListBox Grid.Row="6" Name="lbByEmployee" Height="120" Margin="0,5,0,0" FontSize="14"/>
    </Grid>
</Page>